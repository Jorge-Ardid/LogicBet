# -*- coding: utf-8 -*-
"""Пакет конкурентних прогнозних стратегій logicbet.

Три сектори — `outcomes` / `totals` / `btts` — кожен містить 10 скриптів
`v1.py .. v10.py`, які мають єдиний інтерфейс:

    ID        : унікальний slug стратегії, приклад ``outcomes_v3``
    NAME      : читабельна назва
    SECTOR    : ``"outcomes"`` | ``"totals"`` | ``"btts"``
    predict(ctx) -> {"pick": <selection>, "prob": 0..1} | None

``ctx`` формує :mod:`python/strategy_evaluator` зі стовпців БД, форми команд
та канонічних AI-прогнозів матчу. Самі скрипти **автономні** (лише стандартна
бібліотека), тому їх можна запустити окремо для деталізованого аудиту чи
запуску у зовнішньому пайплайні.
"""

# Ядро конкуренції — набір секторів і їхніх маркетів/варіантів вибору.
SECTORS = {
    "outcomes": {"market": "1X2", "options": ("П1", "X", "П2")},
    "totals": {"market": "TOTAL_GOALS", "options": ("ТБ 2.5", "ТМ 2.5")},
    "btts": {"market": "BTTS", "options": ("ОЗ - Так", "ОЗ - Ні")},
}
